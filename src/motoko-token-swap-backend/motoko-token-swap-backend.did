type FromResult = record {
  subaccount_balance : nat;
  transferred_amount : nat;
  allowance : nat;
  to_transfer : nat;
  to_balance : nat;
};
type Result = variant { Ok : FromResult; Err : text };
service : {
  __get_candid_interface_tmp_hack : () -> (text) query;
  swap : (principal, principal) -> (Result);
}